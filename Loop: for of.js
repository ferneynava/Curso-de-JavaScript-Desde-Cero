// for of: Es un bucle que recorre los elementos de un objeto iterable, como un array, un string, un objeto, un mapa, un set, etc.

for (variable of objeto) {
    // Código a ejecutar
}

//ejemplo
// por (for) cada fruta de (of) la canasta de frutas, imprime la fruta
let canasta = ["manzanas", "peras", "uvas", "naranjas"];

for (let fruta of canasta) {
    console.log(fruta);
}
