const isActive = true; // is (es)
const hasPermisson = false; // has (tiene)

// Conversión implícita 
const resultado = 5 > 3; 
console.log(resultado);

const name = 'Platzi';
console.log(!!name); // !! compronbar si una variable tiene un valor o no

// Conversión explícita
const value = 0;
const otherValue = -24;
const explicitBoolean = Boolean(otherValue);
console.log(explicitBoolean);

